<!--
  EXAMPLE DOMAIN COMPONENT

  This is an example of how to create domain-specific components.
  To use this:
  1. Remove the .example extension from this file
  2. Import it in /lib/theme/domainComponents.ts
  3. Add it to the domainComponents array

  This component will then appear in the Domain Components tab of the theme page.
-->

<script lang="ts">
  export let title = 'Sample Product';
  export let price = '$99.99';
  export let image = 'https://picsum.photos/200/200';
  export let description = 'This is a sample product description.';
  export let inStock = true;
</script>

<div class="product-card">
  <img src={image} alt={title} class="product-image" />
  <div class="product-content">
    <h3 class="product-title">{title}</h3>
    <p class="product-description">{description}</p>
    <div class="product-footer">
      <span class="product-price">{price}</span>
      <span class="stock-status" class:in-stock={inStock} class:out-of-stock={!inStock}>
        {inStock ? 'In Stock' : 'Out of Stock'}
      </span>
    </div>
    <button class="add-to-cart" disabled={!inStock}>
      Add to Cart
    </button>
  </div>
</div>

<style>
  .product-card {
    background: var(--bg-primary);
    border: 1px solid var(--color-gray-200);
    border-radius: var(--radius-lg);
    overflow: hidden;
    transition: all var(--transition-base);
    max-width: 300px;
  }

  .product-card:hover {
    box-shadow: var(--shadow-lg);
    transform: translateY(-2px);
  }

  .product-image {
    width: 100%;
    height: 200px;
    object-fit: cover;
  }

  .product-content {
    padding: var(--space-4);
  }

  .product-title {
    font-size: var(--text-lg);
    font-weight: var(--font-semibold);
    margin-bottom: var(--space-2);
  }

  .product-description {
    color: var(--text-secondary);
    font-size: var(--text-sm);
    margin-bottom: var(--space-4);
  }

  .product-footer {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: var(--space-4);
  }

  .product-price {
    font-size: var(--text-xl);
    font-weight: var(--font-bold);
    color: var(--color-primary);
  }

  .stock-status {
    font-size: var(--text-xs);
    padding: var(--space-1) var(--space-2);
    border-radius: var(--radius-full);
    font-weight: var(--font-medium);
  }

  .stock-status.in-stock {
    background: var(--color-success-light);
    color: var(--color-success-dark);
  }

  .stock-status.out-of-stock {
    background: var(--color-danger-light);
    color: var(--color-danger-dark);
  }

  .add-to-cart {
    width: 100%;
    padding: var(--space-3);
    background: var(--color-primary);
    color: white;
    border: none;
    border-radius: var(--radius-md);
    font-weight: var(--font-medium);
    cursor: pointer;
    transition: all var(--transition-fast);
  }

  .add-to-cart:hover:not(:disabled) {
    background: var(--color-primary-dark);
  }

  .add-to-cart:disabled {
    opacity: 0.5;
    cursor: not-allowed;
  }
</style>